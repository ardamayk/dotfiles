# github personal 
Host me.github
	HostName github.com
	User git
  IdentityFile ~/.ssh/212-prod-ssh
  IdentitiesOnly yes
	# IdentityFile ~/.ssh/me.github

# # Kendi github hesabima girmek icin kullaniyorum
# Host github.com
# 	HostName github.com
# 	User git
# 	IdentityFile ~/.ssh/212-prod-ssh
#   IdentitiesOnly yes

# VPS'e baglanmak icin kullaniyorum, ssh-agent'a eklek' sifre girmeyesin
Host vds-host
  # HostName olarak mailplume.com da yazabiliriz.
  # HostName 45.136.6.147 
  # Normalde olurdu ama sadece tailsclae e izin verecek sekilde ufw ayarlamasi yaptigim icin tailscale ip si gerekiyor.
  # tailscale adresi
  HostName 100.110.58.10
  User deploy
  IdentityFile ~/.ssh/vds-host
  # IdentityFile 
  # ForwardAgent no
  # SecurityKeyProvider /usr/lib/ssh/ssh-sk-helper

# VPS uzerinde githuba a girmek icin passhpraase olan ama SK olmayan ve ayrica localdeki 212 ssh degil de onun yerine farkli bir ssh anahtari kullan. ssh -A vds ile oraya aktar bunu
# forwardagnet yes olmali bunun, EMIN DEGILIM
Host vds-to-github
  HostName github.com
  User git
  IdentityFile ~/.ssh/vds-to-github
